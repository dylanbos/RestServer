var options = require('../lib/options.js');

var opt = {
        "server" : null
};

console.log('test argv: ', process.argv);
var result = options.parse(process.argv.slice(2), opt);
console.log('test result: ', result);

process.argv = [process.argv[1], opt.server].concat(result.end);
var server = require(opt.server);
console.log('test done');